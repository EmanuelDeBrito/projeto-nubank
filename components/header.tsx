import { SafeAreaView, View } from "react-native"

export const Header = () => {
    return(
        <SafeAreaView className="p-6 bg-nubank">
            <View>

            </View>
        </SafeAreaView>
    )
}